{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna bootstrap",
    "dev": "lerna run dev --parallel",
    "preview": "lerna run preview",
    "build": "lerna run build",
    "cfg": "lerna run cfg"
  }
}